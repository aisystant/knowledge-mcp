name = "knowledge-mcp"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
SURREAL_HOST = "https://surrealdb.aisystant.com"

# Secrets (set via `wrangler secret put`):
# OPENAI_API_KEY - for generating query embeddings
# SURREAL_USER - SurrealDB username (also used as namespace)
# SURREAL_PASSWORD - SurrealDB password
